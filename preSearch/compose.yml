services:
  youtube-capture:
    build: .
    image: youtube-capture
    volumes:
      - ./output:/output
    environment:
      YOUTUBE_URL: ${YOUTUBE_URL:?YOUTUBE_URL is required}
      DURATION: ${DURATION:-60}
    restart: "no"
